SELECT item_name
FROM superstore
WHERE price
ORDER BY price;

SELECT SUM(price),category
FROM superstore
GROUP BY category;


SELECT AVG(price),'Kitchen Supplies'
FROM superstore
GROUP BY 'Kitchen Supplies';

